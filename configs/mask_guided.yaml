# Override for mask-guided mode (concat 2 channels)
OUTPUT:
  RUN_NAME: "mask_guided_densenet121"

INPUT:
  MODE: "mask_concat"
  CHANNELS: 2
  IMG_SIZE: 256

MODEL:
  BACKBONE: "densenet121"        

AUG:
  LEVEL: "light"

TEST:
  CKPT_PATH: "${MODEL_PATH}/runs/best.ckpt"  
