# Override for full image mode (single channel baseline)
OUTPUT:
  RUN_NAME: "full_image_densenet121"

INPUT:
  MODE: "full"
  CHANNELS: 1
  IMG_SIZE: 256

MODEL:
  BACKBONE: "densenet121"        

AUG:
  LEVEL: "light"                 

TEST:
  CKPT_PATH: "${MODEL_PATH}/runs/best.ckpt"   
